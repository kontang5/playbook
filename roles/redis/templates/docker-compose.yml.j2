services:
  redis:
    image: {{ redis_image }}
    container_name: {{ redis_container_name }}
    restart: unless-stopped
    ports:
      - "{{ redis_port }}:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
