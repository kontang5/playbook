# OpenObserve credentials (for UI login and initial setup)
ZO_ROOT_USER_EMAIL=admin@example.com
ZO_ROOT_USER_PASSWORD=CHANGE_ME

# OpenObserve ingestion token (for OTel Collector)
# Get from UI after first login: Ingestion -> Data sources -> Copy token
ZO_INGESTION_TOKEN=CHANGE_ME

# PostgreSQL observer user (for metrics collection)
POSTGRES_OBSERVER_PASSWORD=CHANGE_ME
