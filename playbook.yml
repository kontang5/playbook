---
- name: Server setup
  hosts: local
  vars_files:
    - config.yml

  roles:
    - common
    - orbstack
    - role: lmstudio
      when: enable_lmstudio | default(false)
    - jenkins
    - nginx
    - postgresql
    - role: redis
      when: enable_redis | default(false)
    - observability
